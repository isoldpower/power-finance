const EVENTS_HINT = 'Pick the events this endpoint should receive.';

const EVENTS_UNAVAILABLE = "Event types couldn't be loaded.";

const EVENT_SKELETON_ROWS = [0, 1, 2, 3];

const SECRET_NOTICE = 'This secret signs every payload. Copy it now — it cannot be shown again, only rotated.';

const SECRET_COPIED = 'Signing secret copied';

const SECRET_COPY_FAILED = "Couldn't copy the secret";

export { EVENTS_HINT, EVENTS_UNAVAILABLE, EVENT_SKELETON_ROWS };
export { SECRET_COPIED, SECRET_COPY_FAILED, SECRET_NOTICE };
