const RECEIPT_BRAND = 'WHOLE FOODS';

const RECEIPT_BRANCH = 'MARKET · SF';

const RECEIPT_LINES = [
	{ label: 'Bananas', amount: '3.20' },
	{ label: 'Oat milk', amount: '5.49' },
	{ label: 'Sourdough', amount: '6.00' },
];

const RECEIPT_TOTAL = { label: 'TOTAL', amount: '86.40' };

export { RECEIPT_BRAND, RECEIPT_BRANCH, RECEIPT_LINES, RECEIPT_TOTAL };
