const RULE_FIELD_TEXT = "text-[12px] md:text-[12px]";

/* The field name and the matcher read fine truncated; the value does not, so the
   two selects are capped and the value takes whatever is left of the row. */
const RULE_FIELD_WIDTH = "w-[104px] flex-none px-2.5";
const RULE_OPERATOR_WIDTH = "w-[96px] flex-none px-2.5";

export { RULE_FIELD_TEXT, RULE_FIELD_WIDTH, RULE_OPERATOR_WIDTH };
