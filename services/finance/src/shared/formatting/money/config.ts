const DEFAULT_CURRENCY = 'USD';

const DEFAULT_FRACTION_DIGITS = 2;

const FALLBACK_SYMBOLS: Record<string, string> = {
	USD: '$',
	EUR: '€',
	GBP: '£',
	JPY: '¥',
};

export { DEFAULT_CURRENCY, DEFAULT_FRACTION_DIGITS, FALLBACK_SYMBOLS };
