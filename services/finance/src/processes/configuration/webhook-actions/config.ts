const ENDPOINTS_HINT = 'Each endpoint receives a signed POST for the events you pick.';

const ENDPOINTS_EMPTY = 'No endpoints yet. Add one to start receiving events.';

const ENDPOINTS_UNAVAILABLE = "Endpoints couldn't be loaded.";

const ENDPOINT_SAMPLE_SIZE = 2;

const CREATE_HINT = 'Name the endpoint and give the URL that should receive the events.';

const CREATED_HINT = 'Pick the events it should receive from the endpoint list.';

const DANGER_ICON_BUTTON = 'border-[var(--neg-soft)] text-neg hover:bg-neg-soft hover:text-neg';

export { CREATE_HINT, CREATED_HINT, DANGER_ICON_BUTTON };
export { ENDPOINTS_EMPTY, ENDPOINTS_HINT, ENDPOINTS_UNAVAILABLE, ENDPOINT_SAMPLE_SIZE };
